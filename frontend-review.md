### UI Changes in WorkDiary.tsx

- **Background**: Changed from `bg-primary-900` (Dark Blue) to `bg-secondary-50` (Very Light Gray/White).
- **Text Color**: Changed from `text-primary-300` (Light Blue) and `text-white` to `text-secondary-900` (Very Dark Gray/Black) for all text elements:
  - "Custo Total do Dia" label.
  - The total currency value.
  - The "Valores calculados..." description.
- **Icon Styling**: Changed from `bg-white/10` (Semi-transparent white) to `bg-secondary-200 text-secondary-700` for better contrast on the light background.

These changes satisfy the user's request for "letras pretas" (black letters) in the cost summary section.
